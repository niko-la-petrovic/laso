#include "pch.h"
#include "laso.core.h"

namespace laso
{
	namespace core
	{

		bool method(bool test) noexcept
		{
			return test;
		}

		bool init::initMethod(bool test) noexcept
		{
			return test;
		}

	}
}